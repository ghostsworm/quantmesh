# 本地启动脚本（Local helpers）

本目录包含 `dev.sh` / `start.sh` / `stop.sh` / `restart.sh` / `restart_api.sh`，从仓库根挪入此处，减少 GitHub 首页根目录文件数量，便于访客先阅读主 `README.md`。

用法示例（在仓库根目录执行）：

```bash
./scripts/local/dev.sh
./scripts/local/start.sh
./scripts/local/stop.sh
```

详见根目录 [README.md](../../README.md) 中的「快速开始」。
